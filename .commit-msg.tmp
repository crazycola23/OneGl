提高因子分析的可信度，并强化抓取与合规能力

一、统计可信度（把「看起来像结论」和「真的是结论」分开）

- 因子分析改为域名分层：同一域名内配对比较（Wilcoxon signed-rank 对每域名
  的引用率差），主 p-value 与 FDR 全部改用域内口径；合并口径保留为
  pValueNaive 仅供对照。
- 域内差值改为「每域名一票」的平均，不再按方差加权——否则大站会独自决定结论。
- evidenceLevel 同时要求：域内显著、方向一致、且没有域名反向。单向设计
  （某域名只贡献一个分组）无法配对时，一律封顶为「探索性」。
- design.nEff：按二项方差分解估计域内相关，给出真正独立的观察数量级。
  候选行按域名聚集，有效样本量通常远小于候选条数。
- 证据门槛（evidenceGate）：匹配覆盖不足、候选过少、页面证据覆盖率低于 70%
  时，直接不输出优化建议，被抑制的信号单独放进 suppressedSignals 并在 CLI /
  HTML 报告中标注为仅诊断。
- 报告新增未匹配占比与「精确/别名/未匹配」三分，转化率必须与它一起读。

二、抓取与合规能力（page evidence）

- robots.txt：每个域名一次抓取并缓存；被禁路径记为 blocked(ROBOTS_DISALLOW)；
  robots 本身抓取失败时按保守策略跳过该域名，而不是假定允许。
- 域名熔断：连续 3 次失败（blocked/http_error/timeout/redirect_limit）打开
  15 分钟窗口，窗口内其余候选不再发请求；任意成功（含 304）清零。
- 条件请求：命中 etag/last-modified 时发送 if-none-match / if-modified-since；
  304 记为 not_modified，只更新时间戳并保留既有特征，不重读页面。
- 域名确定性间隔（ONEGL_PAGE_DOMAIN_SPACING_MS，默认 1500ms）。
- 内容质量门：200 但无可用正文（JS 壳、插页、正文 <200 字符）记为 unusable，
  不再当成「页面没有 FAQ」。
- CLI 新增 --no-robots / --unconditional，供已确认授权的环境使用。

三、迁移与测试

- 0010：article_page_observations 增加 etag / last_modified，fetch_state 增加
  'unusable'。
- 新增 test/page-fetch-guard.test.mjs（robots 解析与最长匹配、保守同长裁决、
  熔断阈值与窗口、条件请求头、质量门）；重写因子分析测试，覆盖「域内一致
  效应保留等级」「单向设计封顶探索性」「完备配对不可能反向」三类情形。
- 109 项测试全部通过。